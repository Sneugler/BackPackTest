# PreflightGuard Module

PreflightGuard verifies that planned targets and actions remain inside the agreed scope before any validation workflow is run.

Outputs include:
- in-scope vs unknown target checks
- blocked planned actions based on rules of engagement
- a ready/not-ready checklist for authorization evidence, contacts, and scope alignment
