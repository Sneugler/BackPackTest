# EvidenceVault Module

EvidenceVault now supports project-scoped evidence inventory and simple query-based filtering so operators can quickly review stored artifacts and what still needs finding linkage.
